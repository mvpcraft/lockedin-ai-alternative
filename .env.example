# Copy this file to ".env" and fill in your key.
# The app reads the OpenAI API key from here (it takes priority over the UI).

OPENAI_API_KEY=sk-your-key-here

# Optional: override the model (otherwise pick it in the Setup screen).
# OPENAI_MODEL=gpt-4o
